// Package config defines daemon configuration defaults and loading.
//
// The foundation currently returns defaults only. Future file parsing should
// preserve localhost-first security defaults unless explicitly configured.
package config
