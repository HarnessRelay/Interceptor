{
  "name": "harnessrelay-dashboard",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "tsc && vite build",
    "preview": "vite preview --host 127.0.0.1",
    "qa": "npm run build && cd .. && go build -o bin/harnessd ./cmd/harnessd && cd web && NODE_PATH=./node_modules playwright test --config ../playwright.config.ts",
    "qa:headed": "npm run build && cd .. && go build -o bin/harnessd ./cmd/harnessd && cd web && NODE_PATH=./node_modules playwright test --headed --config ../playwright.config.ts",
    "qa:debug": "npm run build && cd .. && go build -o bin/harnessd ./cmd/harnessd && cd web && NODE_PATH=./node_modules playwright test --debug --config ../playwright.config.ts"
  },
  "dependencies": {
    "@vitejs/plugin-react": "^5.0.0",
    "@xterm/addon-fit": "^0.10.0",
    "@xterm/xterm": "^5.5.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "typescript": "^5.8.0",
    "vite": "^7.0.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.0",
    "@types/react": "^19.2.17",
    "@types/react-dom": "^19.2.3"
  }
}
